# رزرو خودکار لایسنس برای همه‌ی سفارش‌ها + آمار پنل ادمین

## چه چیزی الان اتوماتیکه؟

از این به بعد، **چه مشتری کد تخفیف داشته باشه چه نداشته باشه**، وقتی روی
«ثبت سفارش» در فرم خرید سایت بزنه (و حداقل یک محصول نرم‌افزاری/لایسنس‌دار
انتخاب کرده باشه)، سرور لایسنس خودش یک لایسنس واقعی و آماده (`is_used:false`)
براش می‌سازه — دیگه لازم نیست شما دستی چیزی بسازید. فقط سفارش‌های کاملاً
سخت‌افزاری (بدون هیچ لایسنسی) هیچ لایسنسی نمی‌سازن، چون چیزی برای فعال‌سازی
وجود نداره.

## مسیر سرور که این کار رو می‌کنه

`POST /create-order` (جایگزین `/redeem-discount` قبلی شد):

```json
{
  "discountCode": "WELCOME20",   // اختیاری — اگر نبود null بفرستید
  "name": "علی رضایی",
  "email": "ali@example.com",
  "items": [{ "productKey": "p8", "name": "...", "price": 950 }]
}
```

- اگه `discountCode` فرستاده بشه، همون منطق قبلی (چک‌کردن/مصرف‌کردن با
  تراکنش) اجرا می‌شه.
- اگه نباشه، این مرحله رد می‌شه و مستقیم می‌ره سراغ ساخت لایسنس.
- در هر دو حالت، برای هر آیتم نرم‌افزاری یک سند `licenses/{کد جدید}` با
  `is_used:false` و فیلدهای `name`/`email`/`discountCode` (یا `null`) ساخته
  می‌شه، به‌علاوه یک سند در `orders` برای پیگیری.
- یک فیلد جدید هم اضافه شد: `delivered: false` — از پنل ادمین می‌تونید بعد از
  فرستادن کد به مشتری، این رو `true` کنید تا از لیست «در انتظار» جدا بشه.

## تب جدید «لایسنس‌ها» در `admin.html`

### ۱) لایسنس‌های فعال‌شده به تفکیک ماه
از **همه‌ی** اسناد کالکشن `licenses` (چه از سایت اومده باشن چه جای دیگه، مثلاً
با `generate-licenses.js` دستی ساخته شده باشن) میان و بر اساس ماهِ
`activated_at` (یعنی ماهی که مشتری واقعاً لایسنس رو در اپ فعال کرده) گروه‌بندی
می‌کنه؛ برای هر ماه هم مجموع و هم تفکیک طلایی/نقره‌ای/برنزی رو نشون می‌ده — تا
راحت بتونید پورسانت هر ماه رو حساب کنید.

### ۲) لایسنس‌های در انتظار
همه‌ی اسنادی که `is_used:false` هستن (یعنی ساخته شده‌اند ولی مشتری هنوز
فعالشون نکرده) رو لیست می‌کنه: نام، ایمیل، تیر، مدت، کد تخفیف (اگر بود)، و
خودِ **کد لایسنس** (قابل کپی با یک کلیک). این لیست دقیقاً همون چیزیه که
خواستید: «تولید شده ولی هنوز فیشی براش ارسال نشده».

یک دکمه‌ی «علامت‌گذاری به‌عنوان ارسال‌شده» هم کنار هرکدوم هست — بعد از این‌که
کد رو واقعاً برای مشتری فرستادید (بعد از تایید واریزی)، بزنیدش تا فیلد
`delivered` روی سند `true` بشه و بدونید کدوم‌ها رو قبلاً پیگیری کرده‌اید.

> نکته: چون لایسنس‌های ساخته‌شده بدون نام/ایمیل (مثلاً لایسنس‌های عمومی/تریال
> یا هر چیزی که با `generate-licenses.js` دستی ساخته‌اید) هم `is_used:false`
> هستن، توی همین لیست «در انتظار» ظاهر می‌شن ولی با برچسب «بدون نام (لایسنس
> عمومی/دستی)» — چون منتظر واریزی نیستن، لازم نیست نگرانشون باشید.

## Firestore Rules — حتماً قبل از دیپلوی این‌ها رو تنظیم کنید

چون پنل ادمین حالا مستقیماً از کالکشن `licenses` می‌خونه (و برای دکمه‌ی
«ارسال‌شده» آپدیتش هم می‌کنه)، باید این دسترسی فقط برای کاربر لاگین‌شده‌ی ادمین
(Firebase Auth) باز باشه، نه عموم:

```
match /licenses/{code} {
  allow read: if request.auth != null;
  allow update: if request.auth != null
                && request.resource.data.diff(resource.data).affectedKeys()
                     .hasOnly(['delivered', 'delivered_at']);
  allow create, delete: if false; // فقط سرور لایسنس (Admin SDK) می‌سازه
}

match /discountCodes/{code} {
  allow read, write: if false; // فقط سرور، حتی ادمین از طریق کنسول Firebase می‌بینه
}

match /orders/{id} {
  allow read: if request.auth != null;
  allow write: if false;
}
```

## فایل‌های تحویلی

- `license-server-index.js` — جایگزین `index.js` پروژه‌ی `livefx-license-server`
  روی Render کنید و دوباره دیپلوی کنید.
- `index.html` / `script.js` — سایت، با فرم خرید سه‌مرحله‌ای که همیشه سفارش
  را روی سرور ثبت می‌کند.
- `admin.html` — پنل ادمین، با تب جدید «لایسنس‌ها».
